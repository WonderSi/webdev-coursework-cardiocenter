// export function useHeightAnimation(bodyRef: Ref<HTMLElement | null>) {
//   async function animate() { /* логика из SurveyWidget НАДО СДЕЛАТЬ */ }
//   return { animate }
// }
// // Используется в SurveyPage.vue:
// const { animate } = useHeightAnimation(bodyRef)
// watch(currentIndex, animate)