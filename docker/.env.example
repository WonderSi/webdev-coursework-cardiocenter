# ---------------------------
# PostgreSQL
# Эти переменные нужны postgres контейнеру
# чтобы создать БД при первом запуске
# ---------------------------
DB_NAME=cardio_db
DB_USER=cardio_user
DB_PASSWORD=

# ---------------------------
# Redis
# ---------------------------
REDIS_PASSWORD=

# ---------------------------
# JWT
# ---------------------------
JWT_SECRET=

# ---------------------------
# Spring профиль
# prod — внутри Docker
# dev  — локальная разработка
# ---------------------------
SPRING_PROFILES_ACTIVE=prod

# ---------------------------
# ML сервис
# Внутри Docker — имя контейнера
# ---------------------------
ML_SERVICE_URL=http://ml-service:8000

# ---------------------------
# Bootstrap — первый администратор (DOCTOR_EXTENDED)
# Создаётся при первом запуске, если нет ни одного DOCTOR_EXTENDED в БД
# ---------------------------
BOOTSTRAP_ADMIN_EMAIL=
BOOTSTRAP_ADMIN_PASSWORD=