# Курсовая работа по дисциплине "Современные средства и методы веб-разработки"

##  Медицинская информационная система
**Бизнес-контекст:** система для сбора и обработки данных для кардиоцентра

---

### Роли и права:
1. `ADMIN` - авторaизация => вход в админ-панель, внесение данных aaaaaaaпациентов.
    - Импорт и экспорт данных:
        - может загрузить данные из Excel
        - может выгрузить данные в Excel
    - возможно какие-то дашборды в будущем
2. `PATIENT` - обычный посетитель сайта, без авторизации (пока что ТЗ такое), проходит опрос => результаты отправляются в БД.

---

### ДОП. ДАННЫЕ ТЗ:

**Структура данных для автоматической выгрузки Excel-таблиц пациентов (пользователей/опрашиваемых):** 
- `ID`
- `Пол` 
    ```
    {  
        1=м,
        2=ж 
    }
    ```
- `Возраст`
- `Рост`
- `Вес`
- `Алкоголь` 
    ```
    { 
        1 = нет,
        2 = употреблял ранее,
        3 = да, в настоящее время
    }
    ```
- `Курение`
    ```
    { 
        1 = нет,
        2 = бросил,
        3 = курит
    }
    ```
- `Профессия`
    ```
    {
        1 = ведение домашнего хозяйства,
        2 = вооруженные силы,
        3 = лица свободных профессий,
        4 = низкоквалифицированные и неквалифицированные работники, рабочие, ручной труд,
        5 = операторы и монтажники установок и машинного оборудования,
        6 = служащие, сфера обслуживания, работники среднего звена,
        7 = никогда не работающие домохозяйки,
        8 = дипломированные специалисты, умственный труд,
        9 = другое,
        10 = квалифицированные специалисты сельского хозяйства и рыболовного,
        11 = пенсионеры,
        12 = ремесленники и представители других отраслей промышленности,
        13 = техники и младшие специалисты,
        14 = представители законодад. органов власти,  высокопосталенные долж. лица, менеджеры и руководители
    }
    ```
- `Район города или село`
    ```
    {
        1 = рудничный,
        2 = центральный,
        3 = заводский,
        4 = кировский,
        5 = ленинский,
        6 = сельская местность
    }
    ```
- `Наличие хронических заболеваний легких`
- `Наличие онкологии`
- `Наличие сахарного диабета`
- `Глюкоза`
- `Холестерин`
- `Холестерин не-ЛПВП`
- `Холестерин ЛПОНП`
- `Холестерин ЛПВП`
- `Холестерин ЛПНП`
- `Триглицериды`
- `Диагностировался ли инсульт`
    ```
    {
        1 = да,
        2 = нет
    }
    ```
- `Если да, то год диагностирования`
- `Диагностировалась ли сердечная недостаточность`
    ```
    {
        1 = да, 
        2 -нет
    }
    ```
- `Если да, то год диагностирования`
- `Диагностировалось ли нарушение ритма или другие ишемические болезни сердца (ИБС), стенокардия, инфаркт миокарда (ИМ)`
    ```
    {
        1 = да,
        2 = нет
    }
    ```
- `Если да, то год диагностирования`
- `Диагностировалась ли артериальная гипертензия (АГ)`
    ```
    {
        1 = да,
        2 = нет
    }
    ```
- `Если да, то год диагностирования`

### Сущности:

**Для таблиц в БД:** 
- `Patient`
    - `Id: Int`
    - `Create_date: date`
    - `Gender: Boolean`
    - `Age: Int`
    - `Height: numeric`
    - `Weight: numeric`
    - `Alcohol: Int`
    - `Smoking: Int`
    - `Profession: Int`
    - `Region: Int`
    - `Lung diseases: Boolean`
    - `Oncology: Boolean`
    - `Diabetes: Boolean`
    - `Non-HDL Cholesterol: numeric`
    - `IDL Cholesterol: numeric`
    - `HDL Cholesterol: numeric`
    - `LDL Cholesterol: numeric`
    - `Triglycerides: numeric`
    - `User id: Int`
    - `Sys patient id: Int`

- `Diagnosis`
    - `Id: Int`
    - `Patient Id: int`
    - `Diagnosis_id: Int`
    - `Year of diagnosis: Int`

- `Glossary`
    - `Id: Int`
    - `Name: String`
    
- `Glossary value`
    - `Id: Int`
    - `Value: String`
    - `Glossary Id: Int`

<img width="849" height="736" alt="Image" src="https://github.com/user-attachments/assets/e13df9f5-9901-4570-9793-68a7c2faff40" />

Те же сущности, но что стоит за полями:
- `Glossary` – словарь:
    - `Name` – название словаря.

- `Glossary value` – значения словарь:
    - `Value` – значение словаря,
    - `Glossary_id` – ссылка на словарь.

- `Patient` – таблица с данными пациента:
    - `Create date` – дата занесения данных (заносится автоматически в системе),
    - `Пол`,
    - `Возраст`, 
    - `Рост`, 
    - `Вес`,
    - `Алкоголь` (словарь),
    - `Курение`  (словарь),
    - `Профессия` (словарь),
    - `Район города или село` (словарь),
    - `Наличие хронических заболеваний легких`,
    - `Наличие онкологии`, 
    - `Наличие сахарного диабета`, 
    - `Глюкоза`, 
    - `Холестерин`, 
    - `Холестерин не-ЛПВП`, 
    - `Холестерин ЛПОНП`,   
    - `Холестерин ЛПВП`, 
    - `Холестерин ЛПНП`,  
    - `Триглицериды`, 
    - `Диагностировался ли инсульт`,
    - `Если да, то год диагностирования` (первый год, если несколько),
    - `Диагностировалась ли сердечная недостаточность (СН)`,
    - `Если да, то год диагностирования (первый год, если несколько)`,
    - `Диагностировалось ли нарушение ритма или другие ишемические болезни сердца (ИБС), стенокардия, инфаркт миокарда (ИМ)`,
    - `Если да, то год диагностирования (первый год, если несколько)`,
    - `Диагностировалась ли артериальная гипертензия (АГ)`,
    - `Если да, то год диагностирования (первый год, если несколько)`.
    - `User id` - идентификатор занесшего данные пользователя (заносится автоматически в системе),
    - `Sys patient id` – идентификатор пациента (если данные загружены из другой системы). 

- `Diagnosis` – таблица с диагнозами пациентов:
    - `Patient Id` – номер пациента,
    - `Diagnosis_id` – ссылка на диагноз из словаря,
    - `Year of diagnosis` – год постановки диагноза.

---

### Ключевые бизнес-правила:
...

*ПРИМЕР:*

```
1. На один `ScheduleSlot` может быть не более одной активной записи.
2. Запись доступна только на будущие слоты со статусом `OPEN`.
3. Врач не может завершить прием, который не относится к нему.
4. После отмены запись не может быть завершена.
5. История изменения статусов приема должна сохраняться.
```

```
1. Пациент не может повторно пройти опрос с тем же `Sys patiend id`
2. Данные опроса сохраняются атомарно: Либо все поля `Patient` и `Diagnosis` записываются, либо ни одно
3. Импорт Excel выполняется транзакционно - при ошибке вся загрузка откатывается или пропускается с логированием
4. Администратор не може получить доступ к панели без действительного JWT-токена
5. Год диагноза не может быь в будущем и не раньше 1900 года
6. Пациент не может отправить форму с незаполненными обязательными полями
```

---

### Страницы:
```
- Приветственная страница (окошко), выбор `я здесь чтобы пройти опрос` / `я администратор`. Уже после этого переводит на страницу авторизации либо на страницу опросника.
    ├── 4 страницы опросника (возможно изменится)
        └── страница окончания опроса с результатами (вывод => с такой-то вероятностью вы     получите сердечно-сосудистое заболевание ...)
    ├── Страница авторизации
        └── Страница администратора (корректная пара логин-пароль)
            ├── Загрузка данных (импорт из Excel-таблиц)
            └── Выгрузка данных (экспорт в Excel-таблицы)
```

Возможно подключение дашбордов в дальнейшем.

---

### Обязательные пользовательские сценарии (MVP):
- Пациент проходит опрос и получает результат => результат опроса сохраняется в БД
- Администратор авторизовывается
- Администратор может посмотреть статистику по пациентам и их данные (данные из результатов опроса)
    - (в виде какого-то дашборда, возможно)
- Администратор может импортировать/экспортировать данные в формате Excel-таблиц

---

### Минимальный API-контур:
- API для связки с БД (точный стек неопределён со стороны другой команды, ждём информации)
- API для связки с ИИ для анализа данных и прогнозирования заболеваний

*ПРИМЕР:*

```
1. CRUD для `DoctorProfile`, `PatientProfile`, `Specialization`.
2. CRUD для `ScheduleSlot` (с фильтрами по врачу и дате).
3. `POST /appointments` создание записи.
4. `PATCH /appointments/{id}/cancel` отмена записи.
5. `PATCH /appointments/{id}/complete` завершение приема.
6. `GET /appointments` с фильтрами по пациенту, врачу и статусу.
```

### Критерии приемки:
...

*ПРИМЕР:*
```
1. Невозможна двойная запись в один и тот же слот.
2. Ограничения ролей подтверждаются тестами.
3. История статусов приема доступна через API.
```

---

### Таблица распределения

Информационная система предварительного обследования пациентов кардиоцентра на базе веб-опросника
Таблица соответствия темы требования
| Критерии                | Реализация в рамках темы |
|:------------------------|--|
| Роли и доступы          | Пациент: может авторизоваться, протий опрос и узнать свой результат. <br/> Админ/Врач: может просматривать опрос, выгружать опросы и загружать для обучения ML. |
| Сущности и модель       |  |
| Транзакционный сценарий |  |
| Кэшируемый сценарий     |  |
| Фоновый сценарий        |  |
| Безопасность            |  |
| Тестируемость           |  |
| Наблюдаемость           |  |
| Frontend-покрытие       |  |
